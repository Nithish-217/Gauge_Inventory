API_BASE=http://127.0.0.1:5756

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=gaugecalibrationofficial@gmail.com
EMAIL_PASS=ssmn ebot hmhi cqen
EMAIL_FROM="CMTI Gauge Calibration <gaugecalibrationofficial@gmail.com>"

REPORT_STORAGE=minio
MINIO_ENDPOINT=127.0.0.1:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_SECURE=false
MINIO_BUCKET=reports
MINIO_PUBLIC_ENDPOINT=127.0.0.1:9000

# Database Configuration
DATABASE_URL=postgresql+psycopg://postgres:12345678@localhost:2424/GAUGE_INVENTORY
APP_BASE_URL=http://10.37.82.194:3006

# Reminder Configuration - Multiple intervals before due date
REMINDER_INTERVALS=1,3,10

# MinIO Docker Command (for future reference):
# docker run -d -p 9000:9000 -p 9001:9001 --name minio -e "MINIO_ROOT_USER=minioadmin" -e "MINIO_ROOT_PASSWORD=minioadmin" minio/minio server /data --console-address ":9001"
# MinIO Console: http://localhost:9001
# Login: minioadmin / minioadmin
